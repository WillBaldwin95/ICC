let myArr = [];
let mySentence = "I like/love creative coding, so far";
let chopper;
let likelove;

function setup(){
  createCanvas(800,200);
  frameRate(2);
  background(255)
}
function draw(){
  text(mySentence, 50, 100)
}